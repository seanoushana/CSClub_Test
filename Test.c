#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!");
   int first = 5;
   int second = 6;
   int sum = first + second;
   printf("%d", sum);
   return 0;
}